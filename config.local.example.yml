# 本地配置文件 - 包含真实的 API 密钥
# 此文件被 .gitignore 排除，不会提交到版本控制

# 覆盖 DeepSeek API 密钥（测试用）
models:
  deepseek:
    litellm_backend:
      api_key: "sk-test-deepseek-key-from-local-config"
    openai_backend:
      api_key: "sk-test-deepseek-key-from-local-config-openai"

# 覆盖硅基流动 API 密钥
  siliconflow:
    openai_backend:
      api_key: "sk-test-siliconflow-key-from-local-config"

# 覆盖通义千问 API 密钥  
  qwen:
    openai_backend:
      api_key: "sk-test-qwen-key-from-local-config"

# 覆盖通义千问Coder API 密钥
  qwen-coder:
    openai_backend:
      api_key: "sk-test-qwen-coder-key-from-local-config"

# 可以添加其他本地配置
proxy:
  # 本地开发时可能想要更详细的日志
  log_level: "DEBUG"